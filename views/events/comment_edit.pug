extends ../layout

block content
  .container
    h1.main-title Edit comment
    form(action=`/events/comment/${comment.id}`, method="post")
      input(type="hidden", name="_method", value="PUT")
      include _comment_form
      button.btn.btn-outline-primary(type="submit") Update Your Event
